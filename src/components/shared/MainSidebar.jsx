import { useState } from 'react';
import Link from 'next/link';

const MainSidebar = ({ openSidebar, setOpenSidebar }) => {
    return (
        <div className="absolute flex h-screen bg-red-500 w-96 z-10 hidden">

        </div>
    );
};

export default MainSidebar;
